# requirements.txt
# 這個檔案用來定義專案所需的 Python 函式庫
# 在 Colab 或任何其他環境中，可以使用 `pip install -r requirements.txt` 來安裝所有依賴項

# --- 核心數據分析與處理 ---
pandas
numpy

# --- 金融數據下載 ---
yfinance

# --- 回測框架 ---
# Backtesting.py 是一個簡潔且功能強大的回測工具
backtesting

# --- 前端儀表板 ---
# Streamlit 用於快速建立互動式的網頁應用
streamlit

# --- 用於繪圖，是 backtesting 的依賴項 ---
Brotli

# python-binance-related dependencies
aiohttp>=3.8.0
beautifulsoup4>=4.11.0
black>=24.3.0
certifi>=2023.7.22

# yfinance-related dependencies
curl_cffi>=0.5.0

# Plotter visualization platform dependencies
dash[testing]>=2.15.0  # 指定 testing extras
dash-bootstrap-components>=2.0.0
dateparser>=1.1.0

# Plotter module dependencies
flask>=2.3.0
frozendict>=2.3.8
importlib_metadata>=6.7.0
kaleido

# numba-related dependencies
llvmlite>=0.41.0
lxml>=4.9.0

# MAC-related download libraries
# Standard library dependencies
multitasking>=0.0.11

# plotly-related dependencies
narwhals>=0.7.0
numba>=0.58.0
numpy>=1.23.0
openpyxl>=3.1.0

# lo2cin4BT core dependencies
pandas>=2.0.0
pandas-stubs==2.3.0.250703
peewee>=3.16.0
platformdirs>=3.10.0
plotly>=5.0.0
plotly>=6.3.0

# Optional dependencies (for development and testing)
pre-commit>=3.5.0
protobuf>=4.23.0
psutil>=6.3.0
pyarrow>=10.0.0
pycryptodome>=3.17
pytest>=7.0.0
pytest-cov>=4.0.0
playwright
pytest-playwright
python-binance>=1.0.0
python-dateutil>=2.8.2

# pandas-related dependencies
pytz>=2023.3

# Network and data processing dependencies
requests>=2.28.0
rich>=13.0.0
scipy>=1.10.0
statsmodels>=0.14.0
tqdm>=4.66.2
types-pytz==2025.2.0.20250809
types-requests

# requests-related dependencies
urllib3
websockets>=11.0.0
yfinance>=0.2.0
